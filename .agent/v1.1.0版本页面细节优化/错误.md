1. 侧边栏超出视图限制时，提示toast无效，被遮挡
2. 设置-视图管理和分组管理顶部新增搜索
2. 设置-视图管理和分组管理新增：新增视图按钮，新增分组按钮
3. 设置新增关于标签，展示应用信息
4. 快捷卡片出现时，在应用顶部新增一个搜索框，可以根据名称快捷查询视图，搜索框在顶部正中间，与最小化等图标水平对齐
5. 删除图标，还是偶现存在子进程内存占用，请检查。建议删除后做一步追溯进程检测，如果删除的进程还存在 ，再删除一次



1. 快捷卡片出现时，在应用顶部新增一个搜索框，可以根据名称快捷查询视图，搜索框在顶部正中间，与最小化等图标水平对齐。请注意搜索框是与刷新，删除等操作按钮水平对齐

2. 设置-视图管理- 新增视图按钮参照资源路径按钮
3. 设置-分组管理- 新增分组按钮参照资源路径按钮
4. 设置-关于-图标使用resources\icon.png
5. 设置-关于-新增一个服务策略和隐私策略说明弹窗


1. 侧边栏超出视图限制时，toast提示没有显示出来，被webviewContent遮挡了
2. 白色主题时，输入框样式需要兼容
3. 快捷卡片，视图溢出时，左侧第一个图标会被遮挡
4. 视图管理-新增分组-弹窗层级需要比设置层级高
5. 视图管理-新增视图-弹窗层级需要比设置层级高


1. 设置-视图关联-视图新增一个复制图标，点击复制生成一串字符串（视图信息json转码字符串）
2. 打开应用时检测是否有字符串，如果有字符串解析出来，弹新增视图弹窗，引导用户新增视图